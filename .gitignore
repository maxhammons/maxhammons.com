# not part of the site
_archive/
.DS_Store
qa-shots/

# the wget mirror keeps only its HTML and the exported theme CSS; every image is in site/assets
raw/cdn.myportfolio.com/*/*
!raw/cdn.myportfolio.com/*/*.css

# conversion cache (build.py rebuilds it)
raw/derived.nosync/

# build leftovers
*.part
__pycache__/
